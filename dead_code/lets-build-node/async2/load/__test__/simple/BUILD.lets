const { myRule } = load('my_rule.lets');

myRule({
  name: 'generate_file',
  main: '//src/async/load/__test__/noop:noop',
  srcs: ['//src/async/load/__test__/noop:noop2', '//examples/empty:empty-hello-world'],
});

myRule({
  name: 'generate_file2',
  main: 'input.txt'
});
